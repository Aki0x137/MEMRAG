# Memory API Service

FastAPI compatibility layer exposing `POST /api/v1/memories` and
`POST /api/v1/memories/search` for external platforms that want to call MEMRAG's Layer 2
memory surface over HTTP.